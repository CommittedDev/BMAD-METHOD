# Multi-Repo Orchestration Workflow
name: multi-repo-init
description: 'Initialize multi-repo coordination manifest for microservices projects. Use when the user says "setup multi repo" or "configure microservices"'

# Critical variables from config
config_source: "{project-root}/_bmad/bmm/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
user_skill_level: "{config_source}:user_skill_level"
document_output_language: "{config_source}:document_output_language"
date: system-generated
governance_artifacts: "{config_source}:governance_artifacts"
planning_artifacts: "{config_source}:planning_artifacts"

# Workflow components
installed_path: "{project-root}/_bmad/bmm/workflows/multi-repo"
instructions: "{installed_path}/instructions.md"
template: "{installed_path}/manifest-template.yaml"

# Smart input file references
input_file_patterns:
  architecture:
    description: "Architecture for understanding system boundaries"
    whole: "{planning_artifacts}/*architecture*.md"
    sharded: "{planning_artifacts}/*architecture*/*.md"
    load_strategy: "FULL_LOAD"

default_output_file: "{governance_artifacts}/multi-repo-manifest.yaml"
