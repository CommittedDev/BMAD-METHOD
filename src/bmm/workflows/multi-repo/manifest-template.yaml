# Multi-Repo Coordination Manifest
# Generated by BMAD-METHOD Multi-Repo Setup
# Date: {{date}}
# Project: {{project_name}}

project: '{{project_name}}'
generated: '{{date}}'

repositories:
  # - name: "service-name"
  #   path: "/path/to/repo"           # Local path or git URL
  #   role: "api"                       # api, frontend, shared-lib, infra, docs, mobile, worker
  #   technology: "Node.js/TypeScript"  # Primary technology
  #   team: ""                          # Team owner (optional)
  #   dependencies:
  #     - "other-service-name"          # Services this repo depends on
  #   api_contracts:
  #     - "path/to/openapi.yaml"       # API contract files this repo owns

coordination:
  shared_specs: ""                      # Path to shared specification repo or directory
  api_contracts: ""                     # Path to centralized API contract definitions
  deployment_order: []                  # Ordered list of service names for safe rollout
  change_process: |
    # Cross-repo change coordination process:
    # 1. Update shared contracts first
    # 2. Deploy provider services before consumers
    # 3. Use feature flags for breaking changes
    # 4. Run integration tests across affected repos
